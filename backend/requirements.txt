fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
# PostgreSQL driver. The [binary] extra ships a prebuilt wheel, so no
# local C toolchain or libpq install is required on Windows.
psycopg[binary]>=3.2
pydantic>=2.7
python-multipart>=0.0.9
cryptography>=42.0
# tests
pytest>=8.0
httpx>=0.27
